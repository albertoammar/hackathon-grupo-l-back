# Arquivo apenas para configuração do ambiente de desenvolvimento
version: "3"

services:
  app:
    build: .
    environment:
      DB_HOSTNAME: "localhost"
      DB_PORT: "3306"
      DB_NAME: "db"
      DB_USERNAME: "root"
      DB_PASSWORD: "123123aa"
    ports:
      - "8080:8080"